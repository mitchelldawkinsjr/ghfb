# Included in http {} — shared attendance CSV cache for all ghfb-app workers
proxy_cache_path /var/cache/nginx/ghfb
    levels=1:2
    keys_zone=ghfb_attendance:10m
    max_size=64m
    inactive=10m
    use_temp_path=off;
